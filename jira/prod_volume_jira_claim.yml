apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{jiravolumename}}
  namespace: {{namespace}}
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: {{storagesize}}