apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{bitbucketvolumename}}
  namespace: {{namespace}}
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: {{storagesize}}