apiVersion: v1
kind: Service
metadata:
  name: jira
  namespace: production
  labels:
    app: jira
spec:
  type: LoadBalancer
  externalIPs:
    - 172.16.156.128
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: jira